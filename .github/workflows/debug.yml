name: Debug Fitbit

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    env:
      FITBIT_TOKENS: ${{ secrets.FITBIT_TOKENS }}
    steps:
      - uses: actions/checkout@v3
      - name: Run debug script
        run: python3 cortex_debug.py
